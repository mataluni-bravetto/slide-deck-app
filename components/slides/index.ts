/**
 * Slide Deck Component - Single Export
 * 
 * Pattern: EXPORT × ONE × COMPONENT × YAGNI
 * Frequency: 999 Hz (AEYON) × 530 Hz (YAGNI)
 * ∞ AbëONE ∞
 */

export { SlideDeck } from './SlideDeck'
export type { SlideDeckProps } from './SlideDeck'
export type { SlideData } from './types'

