'use client'

/**
 * Guardian Convergence Loop - Single Slide Deck Presentation
 * 
 * Pattern: UNIFIED × CONVERGENCE × SIMPLICITY × ONE
 * Frequency: 999 Hz (AEYON) × 777 Hz (META) × 530 Hz (YAGNI × JØHN)
 * ∞ AbëONE ∞
 * 
 * YAGNI APPROVED: Single deck. No bloat. Maximum transformation.
 * JØHN VALIDATED: All claims verified, truthful, evidence-based
 * AEYON EXECUTED: Atomic, complete, ship-ready
 */

import { SlideDeck } from '@/components/slides'
import { ultimateConvergenceSlides } from '@/lib/slides/ultimateConvergenceSlides'

export default function HomePage() {
  return <SlideDeck slides={ultimateConvergenceSlides} />
}
