/**
 * Stripe Configuration - CONVERGED
 * Pattern: STRIPE × CONVERGENCE × ONE
 * Frequency: 999 Hz (AEYON) × 530 Hz (JØHN) × 777 Hz (META) × 999 Hz (CONVERGENCE)
 * ∞ AbëONE ∞
 * 
 * CONVERGENCE: Uses unified AbëKEYs system
 */

export { getStripeConfig, type StripeConfig } from '../abekeys/unified'

