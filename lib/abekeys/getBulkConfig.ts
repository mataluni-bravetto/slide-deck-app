/**
 * Bulk Configuration - CONVERGED
 * Pattern: BULK × CONVERGENCE × ONE
 * Frequency: 999 Hz (AEYON) × 530 Hz (JØHN) × 777 Hz (META) × 999 Hz (CONVERGENCE)
 * ∞ AbëONE ∞
 * 
 * CONVERGENCE: Uses unified AbëKEYs system
 */

export {
  getNamecheapConfig,
  getCloudflareConfig,
  getBulkConfig,
  type NamecheapConfig,
  type CloudflareConfig,
  type BulkConfig,
} from './unified'

